{
  "type": "object",
  "properties": {
    "taskName": {
      "type": "string",
      "description": "The name of the replication (migrate) task to create."
    },
    "sourceConnectionId": {
      "type": "string",
      "description": "The connection id of the source database to read from."
    },
    "sourceDatabaseType": {
      "type": "string",
      "description": "The database type of the source connection, e.g. MongoDB, Mysql, PostgreSQL, Oracle, SqlServer."
    },
    "targetConnectionId": {
      "type": "string",
      "description": "The connection id of the target database to write to."
    },
    "targetDatabaseType": {
      "type": "string",
      "description": "The database type of the target connection, e.g. MongoDB, Mysql, PostgreSQL, Oracle, SqlServer."
    },
    "migrateTableSelectType": {
      "type": "string",
      "enum": ["custom", "expression"],
      "description": "How to select the source tables to replicate. 'custom' means the user explicitly lists the tables (provide them in 'tableNames'). 'expression' means matching tables by a regular expression (provide it in 'tableExpression'); to replicate ALL tables use the expression '.*'."
    },
    "tableNames": {
      "type": "array",
      "items": { "type": "string" },
      "description": "The explicit list of source table/collection names to replicate. Required when migrateTableSelectType is 'custom'."
    },
    "tableExpression": {
      "type": "string",
      "description": "A regular expression used to match source table names to replicate. Required when migrateTableSelectType is 'expression'. For example 't_.*' matches tables starting with 't_'; use '.*' to replicate all tables."
    },
    "prefix": {
      "type": "string",
      "description": "Optional table rename rule. Add this prefix to every replicated target table name when the user asks to add a table name prefix, e.g. 'F_'."
    },
    "suffix": {
      "type": "string",
      "description": "Optional table rename rule. Add this suffix to every replicated target table name when the user asks to add a table name suffix, e.g. '_E'."
    },
    "replaceBefore": {
      "type": "string",
      "description": "Optional table rename rule. The substring to replace in table names when the user asks to replace characters or text in table names."
    },
    "replaceAfter": {
      "type": "string",
      "description": "Optional table rename rule. The replacement substring used with replaceBefore. For example replaceBefore='a' and replaceAfter='b' changes table name substring 'a' to 'b'."
    },
    "transferCase": {
      "type": "string",
      "enum": ["", "toUpperCase", "toLowerCase"],
      "description": "Optional table rename rule. Use 'toUpperCase' when the user asks to convert table names to uppercase, 'toLowerCase' for lowercase, or '' for no case conversion."
    },
    "tableRenameTableNames": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "originTableName": {
            "type": "string",
            "description": "The original source table name."
          },
          "previousTableName": {
            "type": "string",
            "description": "The previous table name before this rename rule. Defaults to originTableName when omitted."
          },
          "currentTableName": {
            "type": "string",
            "description": "The target table name after renaming."
          }
        },
        "required": ["originTableName", "currentTableName"]
      },
      "description": "Optional exact table rename mappings for rare cases where a specific source table must be renamed to a specific target table. This maps to the table rename node's tableNames configuration."
    },
    "syncType": {
      "type": "string",
      "enum": ["initial_sync", "initial_sync+cdc", "cdc"],
      "description": "The sync type: initial_sync (full sync only), initial_sync+cdc (full + incremental), cdc (incremental only). Default: initial_sync+cdc."
    }
  },
  "required": ["taskName", "sourceConnectionId", "sourceDatabaseType", "targetConnectionId", "targetDatabaseType", "migrateTableSelectType"]
}
