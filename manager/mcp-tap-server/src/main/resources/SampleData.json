{
  "type": "object",
  "properties": {
    "connectionId": {
      "type": "string",
      "description": "The id of the connection to query sample data."
    },
    "schemaName": {
      "type": "string",
      "description": "The name of the data schema to query sample data."
    }
  },
  "required": ["connectionId", "schemaName"]
}