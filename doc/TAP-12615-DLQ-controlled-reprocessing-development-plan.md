# TAP-12615 TapData 异常事件（DQL）与受控重处理开发计划

## 1. 文档说明

- Jira：TAP-12615
- 主题：TapData 异常事件（DQL）与受控重处理 POC
- 编写日期：2026-08-27
- 文档类型：开发计划
- 主要依据：`doc/TAP-12615-DLQ-controlled-reprocessing-detailed-design.md`
- 接口依据：`doc/TAP-12615-DLQ-controlled-reprocessing-api.md`

本文把详细设计转换为可执行、可跟踪、可逐步验收的开发计划。计划只定义开发顺序、依赖、产出和完成标准，不直接承诺日历排期；负责人、开始日期和完成日期应在任务拆分到 Jira 后补充。

### 1.1 开发范围

本计划包含：

- TM 数据模型、存储、API、批次控制、权限校验、告警、配置和初始化。
- Engine 异常分类、风暴保护、记录捕获、DQL 上报和受控重处理。
- TM 与 Engine 的接口联调、故障注入、一致性验证和 POC 验收。

本计划不包含 Web 路由、菜单、页面、组件、前端 API 封装、i18n 和前端测试。Web 功能由其他人员负责；本计划只负责冻结并交付 Web 所需的 TM API、权限码和错误码契约，接口变化需要同步 Web 负责人。

## 2. 状态定义

| 状态 | 含义 |
| --- | --- |
| 已完成 | 步骤的决策、代码或文档产出已经满足完成标准 |
| 已完成（待集成验证） | 当前工作区已有实现并通过相关局部测试，但尚需随完整链路联调验收 |
| 部分完成 | 已有代码骨架或部分能力，仍有设计项、异常分支或测试需要补齐 |
| 未开始 | 当前仓库未发现对应 DQL 专用实现 |
| 待确认 | 开发前需要产品、架构或相关模块负责人冻结口径 |

## 3. 当前开发基线

截至 2026-08-27，按当前工作区进行初步盘点：

| 模块 | 当前状态 | 已有内容 | 主要缺口 |
| --- | --- | --- | --- |
| TM 数据模型 | 部分完成 | DQL 事件、重处理批次 DTO、Entity、Repository 和部分枚举已存在 | 路由分类枚举、字段约束、兼容性及完整测试仍需按设计复核 |
| TM 查询与回调 API | 部分完成 | 上报、后续成功、列表、详情、统计、预览、发起、批次详情和结果回调 Controller/Service 已存在 | 权限口径、校验、幂等、异常补偿和契约测试仍需补齐 |
| TTL 生命周期 | 已完成（待集成验证） | 两个集合已增加 `ttl_at`；创建及重处理活动会刷新；`4.22-7.json` 创建 14 天 TTL 索引 | 需要在真实初始化流程中验证索引安装；历史数据是否回填需上线前决策 |
| 权限 | 已完成（待集成验证） | 已有 `ExceptionEvents` 菜单权限及权限服务；B10 已落实 View-only 定义和任务范围校验；F01 已补 `v2_exception_events` 权限及管理员角色映射初始化 | 需要在真实初始化流程中验证新旧环境升级和 Web 菜单显示 |
| 告警 | 已完成（待集成验证） | 已增加 DQL 告警 key、模板和 `DqlEventAlarmService`；事件创建、保存失败、恢复失败、批次部分失败和 Storm Guard 均已接入真实告警触发点 | 需要在真实 TM/Engine 环境验证通知渠道、模板渲染和告警聚合 |
| Engine 捕获 | 已完成（待集成验证） | 已完成 Engine 侧 DQL 公共模型、TM Client、I/U/D Payload 往返序列化、预览、脱敏、身份生成、异常分类、Storm Guard、目标/处理节点捕获、上报和 C12 回归 | 需要在真实 TM/Engine 联调中验证故障注入、告警和存量任务兼容 |
| Engine 重处理 | 未开始 | 未发现 `dqlRecovery` handler、恢复事件、协调器和 source gate 实现 | 需要完整开发运行中及暂停任务两种回放模式 |
| 集成与 POC | 未开始 | 已有部分 TM 单元测试 | 缺少 Engine、TM/Engine 端到端、故障注入和一致性证明 |

说明：基线状态仅用于安排后续工作，不等同于代码评审结论。每个“部分完成”步骤仍必须按本计划的完成标准重新验收。

## 4. 开发原则和关键依赖

1. 先冻结 TM 与 Engine 契约，并向 Web 负责人交付稳定的 API 契约，避免字段、枚举和状态语义反复变更。
2. Engine 捕获链路必须先完成异常分类，再接入 DLQ 上报；不能先把所有错误上报后再补分类。
3. “DLQ 保存成功才允许 skip”是捕获链路的强一致性边界，任何降级都不能破坏该规则。
4. 重处理链路先完成 TM 批次状态机和锁，再开发 Engine 回放，避免出现无审计的直接回放入口。
5. TM 查询 API 只返回 `RECORD_DLQ` 记录；共享异常继续通过任务状态、任务日志和现有任务告警表达。
6. TTL 使用 `ttl_at`，创建时与 `created` 一致，重处理活动时与同次 `updated` 一致；索引只由 iDaaS 初始化脚本创建。
7. POC 的无重复结论只适用于有主键或唯一键、目标支持幂等或 Exactly-Once 的场景。

## 5. 里程碑总览

| 里程碑 | 范围 | 退出条件 | 依赖 |
| --- | --- | --- | --- |
| M0 契约冻结（已完成） | 状态机、字段、API、配置和错误分类口径 | TM、Engine、Web 对字段及枚举达成一致，契约文档无未决阻塞项 | 无 |
| M1 TM 基础能力（已完成） | 数据模型、存储、查询、上报、权限、TTL | 可通过 API 创建并查询安全的 DQL 主记录，重复上报不重复建档 | M0 |
| M2 Engine 捕获闭环（已完成，待集成验证） | 分类、风暴保护、目标/处理节点捕获、上报 | 确定性单记录错误进入 DQL，共享异常仍走任务级重试；C12 定向回归通过 | M0、M1 |
| M3 TM 批次控制（已完成，待集成验证） | 预览、锁、下发、回调、超时补偿 | 批次状态和事件状态可完整闭环，重复回调不重复计数；TM 定向回归和完整 DQL 回归通过 | M1 |
| M4 Engine 回放闭环（已完成，待集成验证） | 消息处理、源边界注入、屏障、结果回调、失败补偿和回归矩阵 | 运行中和暂停任务均可按服务端顺序逐条回放；5 项 Engine 回放回归通过 | M2、M3 |
| M5 告警与初始化（已完成，待集成验证） | 权限、配置、告警、索引和兼容 | F01-F07 已完成代码级闭环；真实初始化、通知渠道和存量任务升级仍待 G 阶段 | M1-M4 |
| M6 POC 验收（进行中，待外部环境） | TM/Engine 联调、故障注入、一致性证据和文档 | G01-G12 已建立验收步骤，待真实 TM/Engine/Mongo/连接器环境形成通过证据 | M0-M5 |

## 6. 详细开发步骤

### 6.1 阶段 A：契约和技术口径冻结

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| A01 | 已完成 | 冻结 DQL、DLQ 对外命名 | 代码和集合沿用 `dql`；页面统一使用“异常事件”；文档明确 DQL 是本功能内部命名，避免接口和文案混用 | 无 | API、Java 包、Mongo 集合、页面标题和告警文案命名清单确认 |
| A02 | 已完成 | 冻结事件、批次和 attempt 状态机 | 逐项确认允许的状态迁移、可重处理状态、终态、重复回调行为及非法迁移错误码 | A01 | 状态迁移表与代码枚举一致，非法迁移均有明确处理 |
| A03 | 已完成 | 冻结跨端字段和枚举契约 | 对齐 `DqlEventReportVo`、查询 DTO、回放消息、结果回调、snake_case Mongo 字段及 camelCase API 字段；Web 契约以 2026-08-27 API 文档为准 | A01 | TM、Engine 使用完整内部契约；Web 只依赖列表、汇总、详情、预览、提交 5 个接口；API 示例可反序列化 |
| A04 | 已完成 | 冻结异常分类规则 | 确认记录级、共享临时、系统级和未知异常的错误码允许/拒绝列表，明确 `TASK_RETRY` 与 `TASK_ERROR` 的边界 | A02 | 分类规则评审通过，POC 故障注入场景都有预期路由 |
| A05 | 已完成 | 冻结配置和 POC 环境 | 确认批次上限、事件/批次超时、Payload 上限、Storm Guard 阈值、测试连接器和幂等策略 | A04 | 配置默认值、可覆盖方式和 POC 数据集记录在验收清单中 |

### 6.2 阶段 B：TM 数据与基础 API

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| B01 | 已完成 | 补齐领域枚举、DTO、VO 和 Entity | 按详细设计复核事件、批次、attempt、错误类型、异常范围、路由决策、记录身份和报告类型；补齐 BSON 字段映射 | A02、A03 | 编译通过；序列化测试覆盖必填字段、枚举及时间字段 |
| B02 | 已完成 | 完成 `dql_events` Repository | 实现集合创建、普通索引、原子 capture sequence、唯一 upsert、分页过滤、统计、事件锁、attempt 追加和覆盖风险更新 | B01 | Repository 测试覆盖唯一性、排序、条件更新和并发冲突 |
| B03 | 已完成 | 完成 `dql_recovery_batches` Repository | 实现批次创建、状态更新、计数、详情、活动批次查询及状态条件更新 | B01 | 批次计数和状态更新使用原子操作；终态不能被普通回调覆盖 |
| B04 | 已完成 | 完成 TTL 字段生命周期 | 事件和批次创建时 `ttl_at=created`；进入重处理、结果更新、锁释放和批次推进时与同次 `updated` 一起刷新 | B02、B03 | 单元测试证明时间字段同值更新；字段以 BSON Date 保存 |
| B05 | 已完成 | 创建 TTL 初始化脚本 | 使用 `init/idaas/4.22-7.json` 为 `dql_events.ttl_at` 和 `dql_recovery_batches.ttl_at` 创建 14 天单字段 TTL 索引，并更新 `version` | B04 | 初始化流程执行成功；索引 `expireAfterSeconds=1209600`；Repository 不重复创建 TTL 索引 |
| B06 | 已完成 | 完成上报校验和安全处理 | 校验任务、异常范围和路由决策；实现错误详情脱敏与截断、Payload 大小限制、预览脱敏、嵌套深度和条目限制；Engine 回调请求体不进入普通日志 | A03、A05、B01 | 非 `RECORD_DLQ` 显式路由被拒绝；外部查询接口和普通日志不返回完整 Payload；敏感字段测试通过 |
| B07 | 已完成 | 完成事件身份与去重 | Engine 值优先；TM 按 `eventKey`、`payloadHash` 兜底生成 `recordIdentity` 和 `eventIdentity`，使用唯一索引 upsert | B02、B06 | 同一事件重复上报只保留一个主记录，返回 `duplicate=true` 且不重复告警 |
| B08 | 已完成 | 完成 Engine 上报 API | 落实异常事件上报和后续成功写入回调；后者只标记最新未完成前序事件的覆盖风险 | B06、B07 | API 契约测试覆盖首次、重复、非法路由、无匹配后续成功和覆盖风险匹配 |
| B09 | 已完成 | 完成列表、详情和统计 API | 列表支持当前筛选、默认 `-failedAt` 排序和 20 条分页；详情返回安全预览及含 `RUNNING` 的 attempt；统计复用相同权限与非状态筛选，并向 Web 负责人交付契约 | B02、B06 | 查询、统计结果口径一致；事件 ID 只作接口定位；列表和详情均不泄露 `payload_data`；详情可承载 3 秒进度刷新 |
| B10 | 已完成 | 完成任务数据权限 | 查询按可见任务过滤；详情和操作校验事件所属任务；POC 只保留 View 权限，操作复用任务可见性校验 | A03、B09 | 无菜单或无任务数据权限时返回统一错误；跨任务数据不可见 |
| B11 | 已完成 | 统一错误语义和 API 文档 | DQL 接口按参数/权限/资源/状态冲突分别返回 400/403/404/409，未分类异常返回 500；统一 `ResponseMessage` 错误封装和本地化 message，前端按 HTTP 状态处理而不依赖未冻结的内部错误码集合 | B08-B10 | `ExceptionHandler` 状态映射、权限异常、错误消息和 API 文档一致；聚焦契约测试及 A01-B11 回归通过 |
| B12 | 已完成 | 补齐 TM 基础能力测试 | 覆盖 DTO/Entity 映射、Repository、Service、Controller、权限、脱敏、TTL 刷新和并发更新；补充重处理批次闭环及最新 Web 预览契约回归 | B01-B11 | 新增代码的正常路径和关键失败路径均有自动化测试；TM A01-B12 回归 110 项、TM API 错误语义回归 12 项全部通过 |

### 6.3 阶段 C：Engine 捕获与分层路由

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| C01 | 已完成 | 建立 Engine DQL 公共模型和 TM Client | 在 `iengine-common` 定义分类结果、上报请求/响应、后续成功请求、Payload 快照及错误类型映射；Client 复用现有 HTTP 鉴权、重试和错误映射 | A03、B08 | Engine 可调用 TM Mock 接口并正确处理成功、重复、空响应和输入错误；契约字段与路径测试通过 |
| C02 | 已完成 | 实现 Payload 序列化 | 在 `iengine-common` 实现 Insert、Update、Delete 的 `TapRecordEvent` 完整快照和 `tap-record-event-json-v1` 往返转换；使用允许列表、class/type 一致性和反序列化二次大小校验保护回放 | A03、A05 | I/U/D 往返序列化测试通过；按 UTF-8 JSON 字节数执行 1 MiB 默认上限，超限快照移除完整 Payload 并标记 `payloadComplete=false` |
| C03 | 已完成 | 实现预览、脱敏和身份生成工具 | 生成安全 `payloadPreview`、`eventKey`、`payloadHash`、`recordIdentity` 和 `eventIdentity` | C02 | 主键、唯一索引、全字段 hash 和未知身份四类测试通过；敏感字段不出现在预览 |
| C04 | 已完成 | 实现 `DlqExceptionClassifier` | 按异常链、错误码、节点、阶段、事件和任务上下文输出 scope、decision、errorType、reason 和 confidence；当前仅提供纯分类工具，不接入捕获链路 | A04、C01 | 分类规则表中的每条规则至少有一个测试；未知错误不直接默认为记录级 |
| C05 | 已完成 | 实现 `DlqStormGuard` | 在 Engine 公共模块按任务、节点、表、错误码和归一化消息建立线程安全窗口计数，支持数量及批次比例阈值，并返回保护元数据 | A05、C04 | 阈值内允许有限未知单记录入库；触发后停止继续写 DQL 并返回任务级路由；定向测试通过 |
| C06 | 已完成 | 实现 `DqlEventReporter` | 封装 TM 上报、超时、重试边界和结果处理；上报失败必须转任务错误路径 | C01-C05、B08 | TM 不可用或返回错误时当前记录不被 skip；重复响应视为上报成功 |
| C07 | 已完成 | 改造目标写入异常捕获 | 已在 `SkipErrorDataAspect` 目标写入边界接入批量预分类、单条二次分类、Storm Guard、DQL 上报确认和 skip 指标回滚；只有 `RECORD_DLQ` 才上报并 skip | C04-C06 | 目标约束错误进入 DQL；网络/连接异常不拆成大量 DQL，原始异常继续进入现有任务级重试/错误处理路径 |
| C08 | 已完成 | 新增 `SkipErrorProcessAspect` | 已在处理节点 catch 分支暴露输入事件、异常、阶段和处理节点元数据，并由 `SkipErrorEventAspectTask` 注册处理；`RECORD_DLQ` 事件完成 DQL 上报确认后拦截 | C04-C06 | JS 单条转换失败可被拦截；初始化、资源和共享异常仍进入 `errorHandle` |
| C09 | 已完成 | 覆盖 JS 和自定义处理节点 | 已补齐自定义处理器脚本执行异常的 code/cause 保留，并在处理阶段识别嵌套 `ScriptException`；明确区分单记录执行失败、脚本初始化失败和共享资源失败 | C08 | JavaScript/自定义单记录失败进入 DQL；脚本初始化和共享异常不进入 DQL；定向测试与 Engine 构建通过 |
| C10 | 已完成 | 接入后续成功写入回调 | 已在普通目标写入结果观察点按实际写入子批次过滤成功记录，逐条上报身份、事件时间和成功时间，供 TM 标记覆盖风险 | C03、B08 | 只标记同任务、同记录、同运行记录及同表的前序未完成事件；定向测试和 Engine 构建通过 |
| C11 | 已完成 | 修正 skip 指标和失败语义 | 已统一目标写入和处理节点的候选 skip 计数提交/回滚；上报失败、Storm Guard、跳过限制和非记录级路由进入任务级路径；日志区分记录隔离和任务级异常 | C06-C09 | 定向捕获测试 14 项通过，C10/DQL reporter 回归通过，Engine 18 模块构建通过 |
| C12 | 已完成 | 完成 Engine 捕获测试 | 已增加分类器、保护器、序列化、目标捕获、处理节点捕获、上报失败和现有重试回归测试，并补齐禁用 SkipData 的 handler 兼容保护 | C01-C11 | C07-C12 捕获组合 20 项、C01-C06 公共能力 59 项、C09 脚本异常 1 项全部通过；Engine 18 模块构建通过；未开启 SkipData/DQL 的任务行为不变 |

### 6.4 阶段 D：TM 重处理批次与补偿

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| D01 | 已完成 | 完成重处理预览 | 校验同任务、状态、业务键、Payload 完整性、任务状态、版本、Agent 可用性、批次上限和已有活动批次 | A05、B02、B03、B10 | 返回完整 `orderedEvents` 及逐事件阻塞原因；blocked event 含表名、DML、事件时间、捕获顺序和 message；跨任务必须拒绝 |
| D02 | 已完成 | 固化回放顺序 | 服务端按 `task_id ASC, event_time ASC, capture_seq ASC, event_id ASC` 排序并写入批次 `ordered_event_ids` | D01 | 多表、同时间事件排序稳定，预览顺序与实际下发顺序一致 |
| D03 | 已完成 | 实现同任务批次互斥锁 | 使用 `dql_recovery_locks` 独立集合，以 `task_id` 唯一索引和带过期条件的原子 upsert 保证一个任务只有一个 CREATED/DISPATCHED/RUNNING 批次；30 分钟租约支持异常退出后恢复 | B03、D01 | 并发发起时只有一个请求成功；批次创建、事件锁定或下发失败及终态回调均释放锁；异常退出后可在租约过期后重新获取 |
| D04 | 已完成 | 完成事件级锁和批次创建事务语义 | 创建 `CREATED` 批次后条件更新所有事件为 `REPROCESSING` 并写 `current_batch_id`；数量不匹配或锁定异常时按事件原始状态补偿并取消批次；派发前失败时释放事件锁并失败批次 | D02、D03 | 选中数量与锁定数量不一致时不下发；已锁事件恢复原可重处理状态；任务锁在补偿后释放 |
| D05 | 已完成 | 完成 `dqlRecovery` 消息下发 | 新增独立 `DqlRecoveryMessageDto`，携带任务/批次/版本/有序事件/操作人/执行模式，通过 `MessageQueueService` 发送到批次 Agent；批次先置 `DISPATCHED`，不修改已有 DataSync opType | A03、D04 | 消息契约测试通过；下发失败时批次失败、事件按 D04 原始状态解锁、任务锁释放 |
| D06 | 已完成 | 完成 Engine 结果回调状态机 | 已处理 BATCH_STARTED、EVENT_STARTED、EVENT_RESULT、BATCH_FINISHED、BATCH_FAILED；校验任务、批次、事件归属和当前批次锁；EVENT_STARTED 追加 RUNNING attempt，终态回调按结果迁移事件并刷新摘要 | A02、D05 | 回调只能推进合法状态；事件 attempt、摘要和批次计数保持一致；完成前强制满足 `selected=success+failed+skipped` |
| D07 | 已完成 | 实现结果回调幂等 | 已使用 `attemptId + batchId + eventId` 进行事件回调条件更新和重复识别；批次重复回调按当前状态幂等返回 | D06 | 同一回调重复发送不会重复增加 `recovery_count`、success/failed/skipped count，也不会重复追加 attempt 或告警 |
| D08 | 已完成 | 实现批次超时扫描 | 已由带 ShedLock 的 TM 定时任务扫描超时 `DISPATCHED/RUNNING` 批次；为仍为 `REPROCESSING` 的事件追加稳定 `TIMEOUT` attempt，汇总终态并释放锁 | D03、D06、D07 | 超时后不存在永久 `REPROCESSING` 事件或永久任务锁；产生失败告警；重复扫描不重复追加超时 attempt |
| D09 | 已完成 | 完成批次详情和审计 | 返回操作人、模式、任务前后状态、源读取暂停/恢复结果、计数、时间和消息 | D06-D08 | 批次详情 API 可完整表达进度且操作人和结果可追溯；该接口作为可选运维诊断能力，不阻塞当前 Web 联调 |
| D10 | 已完成 | 补齐 TM 重处理测试 | 覆盖并发发起、锁冲突、下发失败、重复回调、部分失败、批次失败和超时恢复 | D01-D09 | 事件数量满足 `selected=success+failed+skipped`，所有终态释放任务锁 |

### 6.5 阶段 E：Engine 重处理执行

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| E01 | 已完成 | 新增 `dqlRecovery` 消息 Handler | 已完成消息解析、任务/版本/Agent/有序事件校验、跨 Handler 进程级批次幂等及协调器启动适配；接受成功后上报 BATCH_STARTED | D05 | 非法或重复消息幂等拒绝；接受成功后上报 BATCH_STARTED |
| E02 | 已完成 | 新增 `TapdataDqlRecoveryEvent` | 已从 `DqlPayloadSnapshot` 重建 I/U/D 原始 DML，复用父类 `eventId` 承载 DQL 事件 ID，保留 `exactlyOnceId`、事件时间、业务字段和 recovery 元数据，并覆盖 clone 复制 | C02、E01 | I/U/D 事件可从存储快照准确重建，Exactly-Once 身份不丢失 |
| E03 | 已完成 | 实现 `DqlRecoveryCoordinator` | 已通过可替换事件源、源边界 sink、屏障和执行策略实现单批次异步串行执行；按顺序读取快照、创建 attempt、注入单条事件、等待结果并上报事件/批次终态 | E01、E02 | 不并发注入同一批次事件；单事件失败时按策略继续或停止；构建与定向测试通过 |
| E04 | 已完成 | 实现运行中任务 `DqlSourceReadGate` | 已在 `HazelcastSourcePdkBaseNode.enqueue` 接入四态闸门；暂停正常源读取但不改变任务业务状态，排空在途入队操作后允许 DQL 回放，并在 finally 恢复读取 | E03 | 回放期间普通源事件不与批次事件交错；成功或异常后均恢复读取 |
| E05 | 已完成 | 实现暂停任务 recovery-only runner | 已以不可变任务快照和仅支持 recovery 入队的 `DqlReplaySourceNode` 构造 runner；不创建正常 TaskClient/Jet Job，不启动 Source reader，不改变暂停状态，并按逆序释放资源 | E03 | 暂停任务可完成回放，任务开始前后状态保持暂停 |
| E06 | 已完成 | 实现源节点边界注入 | 已根据当前任务 DAG 的 `getSourceNodes()` 解析并校验 `DataParentNode` 源边界；恢复事件只允许从已注册源边界进入，缺失或多源歧义均 fail closed；协调器支持 live 源边界工厂 | E02、E04、E05 | 处理节点逻辑与目标写入链完整执行，不允许从目标节点直接写入绕过转换 |
| E07 | 已完成 | 接入逐事件屏障和完成判定 | 已在 source boundary 追加每条事件唯一的 `TapdataCountDownLatchEvent`，以 per-event pending 状态接收目标成功/失败回调；超时、失败和重复完成均为终态，并由协调器形成 EVENT_RESULT | E03、E06 | 下一条事件只在上一条目标结果确定后注入；超时能形成 TIMEOUT/FAILED |
| E08 | 已完成 | 实现恢复防递归捕获 | 已在公共模块增加恢复失败注册表和捕获守卫；目标写入失败、处理节点失败和后续成功回调均识别恢复标记，失败回传原 attempt，不再次创建新的 DQL 主记录 | C07-C09、E02、E07 | 回放失败不进入普通 DQL 上报；原事件屏障收到 FAILED，普通事件捕获行为保持不变 |
| E09 | 已完成 | 实现批次失败补偿 | 已增加批次级幂等补偿器；runner、source gate 恢复和报告/启动回调失败均执行逆序清理，并尽可能发送 BATCH_FAILED；Engine 重启/进程终止仍由 TM 超时扫描兜底 | E03-E08 | 可测试异常路径只执行一次清理；原始失败不被清理或回调异常覆盖；TM 通过回调或超时扫描收敛 |
| E10 | 已完成（待集成验证） | 完成 Engine 回放测试 | 新增回放回归矩阵，覆盖 live、paused、I/U/D、顺序、屏障、单条失败继续、超时、重复消息和 source gate 恢复；Engine 重启后的最终收敛由 D08/E09 的 TM 超时补偿契约承接 | E01-E09 | E10 5 项场景自动化通过；C12 捕获回归通过；任务 start/stop/reset/delete 的真实联调仍需 G08 验证 |

### 6.6 阶段 F：权限、告警、配置和初始化

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| F01 | 已完成（待集成验证） | 统一权限定义并增加初始化 | 按 POC“可见即可操作”采用单一 View；已增加权限资源初始化及升级脚本，并冻结权限码 | A03、B10 | 新旧环境升级后权限一致；无任务权限不能查询或重处理对应事件 |
| F02 | 已完成（待集成验证） | 完成 DQL 告警 key | 已在任务告警枚举末尾补齐进入 DQL、保存失败、恢复失败和 Storm Guard 四个事件型 key；新任务及历史任务均按兼容顺序补充，`4.22-7.json` 幂等 upsert 默认设置 | A04、C05、D08 | 默认告警设置可补齐，既有告警 key 顺序兼容；真实告警发送由 F04 和 G07 验证 |
| F03 | 已完成（待集成验证） | 完成多语言告警模板 | 已增加简中、繁中、英文四项 DQL 模板；占位符限制为任务/事件/批次定位、脱敏错误摘要、保护决策和时间信息，不包含完整 Payload、recordIdentity 或堆栈 | F02 | 三种语言模板契约测试通过；任务、事件或批次定位准确；真实告警渲染由 F04/G07 验证 |
| F04 | 已完成（待集成验证） | 接入真实告警触发点 | 已在 DQL 主记录首次落库、保存失败、单事件/批次失败、批次部分失败和 Storm Guard 触发点调用 `AlarmService`；共享异常继续复用现有任务级告警；告警发送失败不改变 DQL 持久化、回放和任务路由 | B07、C05-C07、D06-D08、F02 | 重复上报不重复告警；告警仅使用安全摘要；Storm Guard 上报仅含摘要和哈希维度；告警后端异常不阻断业务流程；聚焦测试通过 |
| F05 | 已完成（待集成验证） | 增加系统配置及读取逻辑 | 已由 `DqlRuntimeConfig` 统一 14 项 DQL 配置的默认值、优先级、合法范围和快照读取；TM/Engine 已接入，4.22-7 初始化脚本已增加幂等 Settings upsert | A05 | 默认配置安装成功；非法值有保护；配置变化范围和生效方式有文档 |
| F06 | 已完成（待集成验证） | 验证数据库初始化和升级兼容 | 已通过正式 `4.22-7.json` 结构检查及 `PatchesRunner -> ScriptPatchScanner -> JsonFilePatch` 测试覆盖空索引、匹配索引重复执行、Settings 幂等和历史无 `ttl_at` 回填结论；真实 MongoDB 仍待集成环境验证 | B05 | 初始化可重复执行；两个 TTL 索引配置正确；历史数据回填方案有明确结论 |
| F07 | 已完成（待集成验证） | 完成前后兼容回归 | 已统一保护 Engine DQL 捕获入口；验证 DQL 关闭、SkipTable、SkipData、SkipTableForMigrateSnapshot、非迁移任务和旧任务告警路径，未改变 SkipErrorTable 实现 | C12、E10、F01-F06 | 本地兼容回归通过；真实存量任务和升级验证由 G05/G08/G09/G12 承接 |

### 6.7 阶段 G：联调、故障注入和 POC 验收

| 步骤 | 状态 | 需要完成的事项 | 说明 | 依赖 | 完成标准 |
| --- | --- | --- | --- | --- | --- |
| G01 | 待外部环境 | 建立端到端测试环境和数据集 | 已确认 `docker-compose.local.yml` 的 Mongo/TM/Engine 拓扑并固化数据集、故障注入和证据要求；当前 Docker daemon 无权限，真实启动待外部环境 | M1-M5 | 环境、任务配置、样例数据和预期结果可重复创建 |
| G02 | 待外部环境 | 验证确定性记录错误捕获 | C12 已覆盖本地确定性捕获和 20 项组合回归；真实格式/长度/非空/唯一约束、Poison Record 和 JS 注入待 G01 环境 | G01 | 每个唯一异常事件只产生一个 DQL 主记录；正常数据继续同步 |
| G03 | 待外部环境 | 验证共享异常任务级重试 | Engine Client/Reporter、C12 已覆盖共享异常分流；真实断网恢复、重试耗尽、旧任务状态和告警观察待 G01 环境 | G01 | 临时故障走任务级重试并从原进度继续；期间不批量新增 DQL；耗尽后任务错误并告警 |
| G04 | 待外部环境 | 验证未知异常风暴保护 | `DlqStormGuard` 及 Engine 公共 Client/Reporter/Guard 组合 30 项本地通过；真实批量未知异常、窗口阈值、日志和告警观察待 G01 环境 | G01 | 保护触发后窗口内不持续新增 DQL，日志和告警含 guardKey、阈值及抑制数量 |
| G05 | 待外部环境 | 验证查询 API、权限和安全 | B09-B11/F01 已冻结接口、权限范围、分页排序和安全预览；真实多账户 HTTP 响应、升级数据和 Mongo 结果待 TM 环境 | G02 | 汇总与非状态筛选一致；无越权数据；API 响应无 `payload_data`；事件 ID 不需要作为页面展示字段 |
| G06 | 待外部环境 | 验证单条和批量重处理 | E01-E10 已完成 Engine 回放实现，E10 矩阵 5/5 通过；真实运行中/暂停任务、连接器 I/U/D、TM 批次状态和跨任务校验待联调 | G02、G05 | 状态、含 `RUNNING` 的 attempt、批次计数、任务状态和源读取恢复结果符合设计；Web 可只靠详情历史查看进度 |
| G07 | 待外部环境 | 验证顺序、无重复和无丢失证据 | B07、E06-E10/C12 已具备身份、排序、屏障和重复消息本地证据；真实源/目标日志、目标去重和数量对账待 G06 联调 | G06 | 顺序满足设计；幂等适用范围内目标无重复；对账公式全部成立 |
| G08 | 待外部环境 | 验证失败补偿和幂等 | D04-D08/E09 已覆盖锁、重复回调、超时和逆序补偿的本地实现；真实 TM/Engine 重启、网络/消息故障和锁收敛待外部环境 | G06 | 无永久事件锁或任务锁；重复回调不重复计数；批次最终收敛到终态 |
| G09 | 待外部环境 | 验证 TTL | F06 已通过正式初始化结构、14 天 TTL 和幂等检查；真实 Mongo TTL Monitor 清理、重处理刷新和升级前后索引查询待外部环境 | F06、G06 | 未活动数据按 TTL 清理；发生有效重处理活动的数据从最新 `ttl_at` 重新计时 |
| G10 | 待外部环境 | 执行本计划范围需求检查 | 已建立 A-F 步骤与 G02-G09 证据索引及偏差框架；真实接口、Mongo、日志、告警、锁收敛和数量对账证据待补，Web 页面不在本计划 | G02-G09 | TM、Engine、初始化和后端权限相关需求均有通过证据或明确偏差结论 |
| G11 | 待外部环境 | 整理 POC 演示和已知限制 | 已固化 TM/Engine 演示脚本、恢复步骤、观测点、对账公式、支持边界和不做内容；真实演示日志/告警/数据仍待 G01-G10 环境证据 | G10 | 可重复演示本计划范围链路；限制与不做内容无歧义 |
| G12 | 未开始 | 完成发布前检查 | 代码评审、静态检查、模块测试、集成测试、初始化验证、回滚方案和文档同步 | G01-G11 | 所有阻塞问题关闭，发布清单签字，版本升级路径可执行 |

## 7. 建议的并行开发方式

契约冻结后可按以下工作流并行：

| 工作流 | 可并行范围 | 必须等待的接口点 |
| --- | --- | --- |
| TM 基础 | B01-B12 | A03 字段契约、A05 配置默认值 |
| Engine 捕获 | C01-C12 | B08 上报 API 契约；真实联调等待 M1 |
| TM 重处理 | D01-D10 | A02 状态机、A03 消息与回调契约 |
| Engine 回放 | E01-E10 | D05 消息契约、D06 回调契约；真实联调等待 M3 |
| 告警和初始化 | F01-F07 | 权限、配置和告警口径冻结后可与主链路并行 |

关键路径为：

```text
A03 契约冻结
  -> B08 TM 上报 API
  -> C04-C09 Engine 分类与捕获
  -> G02/G03 捕获联调
  -> D01-D07 TM 批次控制
  -> E01-E09 Engine 回放
  -> G06-G08 重处理与补偿验收
  -> G10-G12 POC 和发布检查
```

Web 由其他人员按独立计划推进。本计划通过 A03、B09、B11、D01 和 D04 提供当前 Web 所需的字段、查询、HTTP 错误语义、预览和提交契约，并在契约变化时主动同步。D09 的批次详情属于可选运维诊断能力，不是当前 Web 的联调前置条件。

## 8. 每步统一完成定义

除步骤自身的完成标准外，每个开发步骤还必须满足：

1. 代码与详细设计一致；若实现调整设计，必须先同步相关设计和接口文档。
2. 新增公共字段、枚举、错误码、配置或消息时，TM、Engine 和文档同步更新，并通知 Web 负责人评估接口影响。
3. 正常路径、关键异常路径、幂等和权限边界具备与风险相匹配的自动化测试。
4. 不在外部查询响应、普通日志和告警中输出完整 Payload、密码、Token 或未脱敏错误信息。
5. 不改变未启用 DQL 的任务、现有任务级重试、SkipTable 和 TaskSkipErrorTable 的既有语义。
6. 数据更新涉及状态、`updated`、`ttl_at`、批次 ID 或计数时，应使用原子条件更新并验证修改数量。
7. 新增初始化脚本可重复执行，升级和回滚影响明确。
8. `git diff --check`、模块编译、相关单元测试和契约测试通过。

## 9. POC 验收证据清单

| 证据 | 需要记录的内容 |
| --- | --- |
| 异常分类 | 原始错误、分类结果、routeDecision、classificationReason、任务最终行为 |
| DQL 持久化 | `event_id`、唯一身份、状态、Payload 完整性、安全预览、`ttl_at` 和索引 |
| 共享异常 | 任务重试日志、恢复点、任务状态、告警，以及故障期间 DQL 新增数量 |
| Storm Guard | guardKey、窗口、阈值、允许数量、触发时间、抑制数量和任务级路由 |
| 查询权限 | 不同用户调用查询、详情和重处理 API 的授权结果，以及跨任务拒绝结果 |
| 回放顺序 | preview 顺序、批次 orderedEventIds、Engine 注入日志、屏障完成日志和目标落库顺序 |
| 无重复 | eventIdentity 去重结果、recoveryCount、目标主键/唯一键结果和 Exactly-Once 标识 |
| 无丢失 | 输入、首次成功、DQL、已恢复、未恢复以及批次计数对账 |
| 失败补偿 | 下发失败、回调重复、Engine 重启、超时后的事件状态、批次状态和锁状态 |
| TTL | 索引定义、创建时间、重处理刷新时间以及过期清理结果 |
| 告警 | DQL 创建、保存失败、恢复失败、Storm Guard 和共享异常现有告警样例 |

## 10. 上线前必须明确的决策

以下事项不应由开发人员在实现中自行推断，必须在进入 M6 前明确：

1. POC 权限最终采用单一 View，还是保留独立 Edit 权限；当前代码和详细设计口径需要统一。
2. Storm Guard 触发后使用专用 `TASK_DQL_STORM_GUARD`，还是合并到现有任务错误告警。
3. 同任务互斥锁使用独立 `dql_recovery_locks` 集合，还是使用现有分布式锁能力。
4. 暂停任务 recovery-only runner 支持的任务类型、节点类型和连接器范围。
5. 历史 `dql_events`、`dql_recovery_batches` 是否存在；如存在，是否在上线前将 `created` 回填到 `ttl_at`。
6. DQL 配置是进入 Settings 初始化脚本、部署配置还是任务配置，以及哪些配置支持运行时调整。
7. POC 用于证明无重复的目标连接器、主键/唯一键、写入策略和 Exactly-Once 配置。

以上决策如果未冻结，不影响 Mock 开发，但会阻塞完整联调和最终验收。
