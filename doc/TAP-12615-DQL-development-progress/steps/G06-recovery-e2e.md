# G06 单条和批量重处理

## 状态

- 状态：待外部环境
- 分析日期：2026-08-28
- 依赖：G02、G05

## 分析结论

E01-E10 已完成 Engine 回放链路：消息校验、批次启动、运行中任务 source gate、暂停任务恢复、I/U/D 事件重建、逐事件屏障、单条失败继续、恢复防递归和最终回调均有代码与定向回归。E10 的回放矩阵记录为 5/5 通过。

真实 G06 需要从 TM 预览排序后提交单条和批量回放，分别覆盖运行中/暂停任务、跨表同任务、非法跨任务、I/U/D 和目标失败继续，观察批次计数、`RUNNING` attempt、任务状态和源读取恢复。当前无可运行 TM/Engine/连接器实例，不能完成端到端状态证据。

## 验收证据

- 预览与提交使用同一固定排序，批次只包含授权任务的事件。
- 单事件成功/失败/超时均形成终态；批次计数、任务状态和回调一致。
- 回放后普通源读取恢复，失败事件不会递归产生新的 DQL 主记录。

## 外部前置

需要 G01 环境、至少一个运行中和一个暂停任务、可写源/目标连接器以及 TM API 账户。完成后继续 G07。
