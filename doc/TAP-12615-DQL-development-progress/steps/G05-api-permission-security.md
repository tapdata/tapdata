# G05 查询 API、权限和安全

## 状态

- 状态：待外部环境
- 分析日期：2026-08-28
- 依赖：G02

## 分析结论

TM 侧已经完成 DQL 列表、详情、统计、当前筛选、预览和重处理接口，B09-B11 冻结了分页、`-failedAt` 默认排序、HTTP 错误语义和安全预览边界；B10/F01 还完成了任务范围校验、View-only 权限和权限初始化。Payload 完整数据不作为列表/详情页面响应字段，安全预览负责脱敏。

真实 G05 仍需使用不同任务权限账户调用 HTTP API，验证 20 条分页、筛选汇总一致、无越权、400/403/404/409 映射、事件 ID 不泄露为页面展示字段，以及新旧环境权限升级。当前无可访问 TM 实例和账户，无法完成线上响应证据。

## 验收证据

- 有权限/无权限/跨任务账户的 API 响应和 Mongo 任务范围对照。
- 列表、详情、统计与筛选条件的数量对账，默认按 `failedAt` 倒序。
- 响应 JSON 不包含 `payload_data`；安全预览只包含脱敏后的必要字段。

## 外部前置

需要 G01 的 TM/Mongo 实例、至少两个权限边界账户和可抓取 HTTP 响应的客户端。完成后继续 G06。
