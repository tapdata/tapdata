# G08 失败补偿和幂等

## 状态

- 状态：待外部环境
- 分析日期：2026-08-28
- 依赖：G06

## 分析结论

D04-D08 已覆盖独立事件锁、同任务批次锁、下发失败、回调状态机、重复回调、批次超时和详情审计；E09 增加了 runner、source gate、报告/启动回调失败时的逆序清理，TM 超时扫描作为 Engine 重启/进程终止的兜底。补偿和状态转移的本地测试已完成。

真实 G08 需要在 TM 不可用、消息下发失败、Engine 重启、重复消息/回调、回放超时和 source gate 恢复失败等时间序列中检查没有永久事件锁/任务锁，且批次最终收敛到终态。当前无法控制外部进程和网络，不能完成真实故障注入。

## 验收证据

- 每个故障注入点记录锁、批次、事件和任务状态的前后快照。
- 重复消息/回调只产生一次状态迁移和一次计数变化。
- 补偿异常不覆盖原始失败；锁最终释放，批次由回调或超时扫描收敛。

## 外部前置

需要可重启的 TM/Engine 容器、可控消息/网络故障、Mongo 锁集合查询权限和可调短超时配置。完成后继续 G09。
