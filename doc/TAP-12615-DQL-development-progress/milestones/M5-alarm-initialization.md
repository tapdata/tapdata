# M5 告警与初始化里程碑

## 状态

- 状态：已完成（待集成验证）
- 完成日期：2026-08-28
- 范围：F01-F07

## 里程碑结论

权限、告警、运行时配置、数据库初始化/升级兼容和前后兼容回归已经完成代码级闭环。DQL 的失败路径具备安全摘要告警，配置读取具备默认值、范围保护和任务启动快照，两个 DQL 集合具备 TTL 初始化；旧 SkipErrorTable、共享异常任务级重试和任务告警路径保持独立。

## 完成矩阵

| 步骤 | 结果 | 关键证据 |
| --- | --- | --- |
| F01 | 已完成 | 权限资源、角色映射和升级初始化 |
| F02 | 已完成 | 四项 DQL 告警 key 和幂等默认设置 |
| F03 | 已完成 | 简中、繁中、英文安全模板 |
| F04 | 已完成 | 创建、保存失败、恢复失败、部分失败和 Storm Guard 触发点 |
| F05 | 已完成 | 14 项运行时配置、默认值/范围/快照语义 |
| F06 | 已完成 | 正式 `4.22-7.json`、TTL 索引、Settings 幂等和无历史回填 |
| F07 | 已完成 | DQL 开关、旧 SkipErrorTable 模式和任务级旧告警兼容 |

## 验证与未决项

本地已完成 F01-F07 的定向测试、初始化结构检查、脚本扫描和 Engine 兼容回归。以下项目必须在 G 阶段真实环境完成后才能将 M5 标记为无条件通过：

- 新旧 Mongo 数据库执行初始化/升级，确认两个 TTL 索引和 14 项 Settings 实际落库。
- TM/Engine 联调验证告警模板渲染、通知渠道、重复抑制和旧任务告警。
- 存量任务验证 DQL 关闭、SkipData、SkipTable 和迁移快照模式不改变旧路由。
