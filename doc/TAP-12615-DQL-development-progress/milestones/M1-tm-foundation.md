# M1 TM 基础能力里程碑总结

## 里程碑信息

- 状态：已完成
- 完成日期：2026-08-27
- 包含步骤：B01-B12
- 前置里程碑：M0 契约冻结

## 完成结果

| 领域 | 完成结果 |
| --- | --- |
| 数据模型与存储 | 事件、批次、Attempt、Payload、身份、TTL 和路由字段已落地；`dql_events` 与 `dql_recovery_batches` 的 Repository 支持条件更新、排序、统计和锁语义 |
| 上报与去重 | Engine 上报、重复事件识别、后续成功覆盖风险和批次下发基础能力已完成；重复上报不重复建档 |
| 查询与安全 | 列表、详情、汇总、预览和批次诊断接口已实现统一权限与错误语义；完整 Payload、内部身份和未脱敏错误不进入对外查询或普通日志 |
| 批次基础能力 | 批次创建、预览、事件锁、计数、状态收敛、结果回调和失败补偿基础路径已回归验证 |
| 生命周期与初始化 | 事件和批次的 `ttl_at` 生命周期已统一；`4.22-7.json` 提供 14 天 TTL 索引，Repository 不自行创建 TTL 索引 |
| 权限与错误 | View-only 任务数据权限、跨任务校验和 400/403/404/409/500 DQL 错误语义已冻结并回归 |

## 步骤证据

- B01-B06 完成领域契约、Repository、TTL、初始化、校验和安全处理，详见对应 `steps/B01-*.md` 至 `steps/B06-*.md`。
- B07-B11 完成身份去重、上报、查询、任务权限和错误语义，详见对应 `steps/B07-*.md` 至 `steps/B11-*.md`。
- B12 将 TM 基础能力纳入整体回归，并补齐批次 Service、Web 映射和 DTO/Entity 映射测试，详见 `steps/B12-tm-foundation-regression.md`。

## 退出条件检查

- 可通过 API 创建并查询安全的 DQL 主记录，查询结果遵守任务数据权限和 Payload 安全边界。
- 重复上报使用 Engine 身份优先、TM 兜底和唯一 upsert，不重复创建主记录或告警。
- 事件、批次、Attempt、锁、计数、TTL 和回调的关键正常/失败/幂等路径已有自动化回归。
- TM A01-B12 DQL 回归 110 项、TM API 错误语义回归 12 项全部通过；对应构建成功。

## 里程碑边界

- Engine 捕获的分类、风暴保护、处理节点捕获和上报编排尚未完成，属于 M2。
- TM/Engine 真实联调、回放批次闭环、故障注入和 POC 证据尚未完成，不因 M1 完成而提前宣称端到端可用。
- Web 页面不在本计划范围；本里程碑仅交付其所需的后端 API、权限码和错误语义契约。

## 下一里程碑

M1 完成后，阶段 C 继续按 C01-C12 推进。C01-C03 已完成并提交 review；下一步等待用户 review 与验证，再进入 C04 异常分类器开发。
