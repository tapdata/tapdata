# M3 TM 批次控制

状态：已完成（待 TM/Engine 集成验证）  
完成日期：2026-08-28  
范围：D01-D10

## 退出条件

M3 的 TM 侧退出条件已经满足：

- 预览返回可提交事件和稳定回放顺序。
- 同一任务通过独立租约锁保证只有一个活动批次。
- 批次创建、事件锁定、消息派发和失败路径具备条件更新与补偿语义。
- Engine 回调覆盖批次开始、事件开始、事件结果、批次完成和批次失败，并校验批次/事件归属。
- 重复回调不会重复追加 attempt、计数、告警或释放锁。
- 超时扫描能补偿未完成事件并最终释放任务锁。
- 批次详情包含操作人、模式、任务前后状态、源读取暂停/恢复结果、计数、时间、消息和审计时间线。
- TM 定向批次回归 81 项、完整 DQL 回归 130 项全部通过。

## 尚待验证

M3 仍标记“待 TM/Engine 集成验证”，因为 Engine E01-E10 尚未完成真实消息、回放、source gate 和故障注入联调。当前结论只表示 TM 批次控制的本地实现和自动化回归达到阶段退出标准，不提前宣称端到端回放可用。

## 后续里程碑

进入 M4 前，E01-E09 需要复用 D05 消息契约、D06/D07 回调契约和 D09 source gate 审计入口，完成运行中及暂停任务的 Engine 回放闭环；E10 再执行 Engine 侧场景回归。
