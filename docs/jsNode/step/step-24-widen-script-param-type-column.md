# 开发步骤 24：扩大脚本参数类型列

## 实现目标

将“类型”列宽度相对现有布局增加 50%，让类型选择框拥有更充足的显示空间，同时保持表格总宽度不变。

## 实现

- “类型”列由 15% 调整为 22.5%。
- “参数值”列由 43% 调整为 35.5%，抵消新增宽度。
- 参数名、加密和操作列宽度保持不变。

## 验收

- 表格列宽总和仍为 100%。
- 类型选择框宽度增加，其他配置功能不受影响。
- 变更文件通过格式检查，脚本参数单测保持通过。

