# TAP-12568 浮点数适配开发总结

本文档按研发计划表记录每个可验收步骤的实际完成情况。每条记录对应一个本地 Git 提交，提交信息统一使用 `feat(TAP-12568): ...` 格式。

## P-01 基线、契约与计划审核

- 状态：已完成
- 完成内容：完成概要设计、详细设计、研发计划表；确认 `TapFloat`、`TapDouble` 为 `TapType` 的直接子类型，入站值统一以 `Double` 承载；保留 `TapNumber` 作为旧模型兼容类型；确认 74 个数据源 spec 独立修复。
- 审核结论：不设置“第一阶段/第二阶段”尾项，计划覆盖类型、值对象、编解码、推演、序列化、兼容、数据源 spec、验证和产品文档；每个数据源均为独立可验收步骤。
- 验证：计划中的数据源路径全部存在；计划行号、源标识无重复；Markdown fence 成对；文档无未决 TODO/TBD。
- 提交：`tapdata` `ddc15883db feat(TAP-12568): add floating point design and development plan`

## S-01 至 S-17 企业连接器 spec 修复

以下每个数据源均独立完成“官方定义核对、spec 修改、JSON/静态门禁验证、独立提交”，没有使用跨数据源的兼容性兜底：

- S-01 ApsaraDB RDS SQL Server：`real` 改为 binary32；`float(p)` 使用 p=1–24/25–53 resolver，默认 p=53 为 `TapDouble`。提交：`tapdata-connectors-enterprise` `1e51fc66 feat(TAP-12568): fix aliyun rds mssql floating point spec`
- S-02 Dameng：`REAL`/`BINARY_FLOAT` 为 binary32，`DOUBLE`/`BINARY_DOUBLE`/`DOUBLE PRECISION` 为 binary64。提交：`39960cba feat(TAP-12568): fix dameng floating point spec`
- S-03 Db2：`REAL` 为 32/4，`DOUBLE` 为 64/8，删除 scale。提交：`83fd3fa5 feat(TAP-12568): fix db2 floating point spec`
- S-04 Db2 for i：`FLOAT(p)` 按 1–24/25–53 分流，`DOUBLE` 为 binary64。提交：`3f2331a7 feat(TAP-12568): fix db2 for i floating point spec`
- S-05 GBase 8a：按 MySQL 兼容定义修复 FLOAT/DOUBLE 及 M/D 形式。提交：`683febcb feat(TAP-12568): fix gbase8a floating point spec`
- S-06 GBase 8s：`SMALLFLOAT` 更正为 32/4 的 `TapFloat`，`FLOAT` 为 64/8 的 `TapDouble`。提交：`fdb81c9d feat(TAP-12568): fix gbase8s floating point spec`
- S-07 SAP HANA：`REAL`/`DOUBLE` 分别为 binary32/binary64。提交：`de21624f feat(TAP-12568): fix hana floating point spec`
- S-08 IBM Informix：依据官方定义更正 `SMALLFLOAT` 的错误 bit=16，`FLOAT(p)` 按 double precision 64/8 处理。提交：`07906aa0 feat(TAP-12568): fix informix floating point spec`
- S-09 IRIS：`double` 改为 binary64，移除 precision/scale。提交：`9c43cfda feat(TAP-12568): fix iris floating point spec`
- S-10/S-11 KingbaseES V8R3/V8R6：按 PostgreSQL 兼容定义分别修复 `real`/`double precision`。提交：`779c1dad`、`1e05273e`
- S-12 SQL Server：`real` 更正为 32/4，`float(p)` 按 1–24/25–53 分流，明确 float(53) 为 `TapDouble`。提交：`6cab482f feat(TAP-12568): fix sql server floating point spec`
- S-13/S-14 OceanBase Oracle/Oracle：仅 `BINARY_FLOAT`/`BINARY_DOUBLE` 改为新类型；Oracle `FLOAT(p)` 保持 `TapNumber` 并由 allowlist 记录为 NUMBER 子类型。提交：`5def8e3a`、`6dee1af1`
- S-15 Sybase：`float(p)` 增加 binary precision resolver，`real`/`double precision` 分别为 32/64。提交：`8d892b9f feat(TAP-12568): fix sybase floating point spec`
- S-16 TencentDB SQL Server：独立重复 SQL Server 定义核验，修复 64 位误标风险。提交：`94c65f78 feat(TAP-12568): fix tencent sql server floating point spec`
- S-17 TDSQL MySQL：按 MySQL FLOAT/DOUBLE/M,D 语义独立修复。提交：`f62d4a80 feat(TAP-12568): fix tencent mysql floating point spec`

- 验证：上述 17 个 spec 均通过 JSON 解析和静态检查器，共检查 43 个浮点候选；`git diff --check` 通过。

## S-18 至 S-74 公共连接器 spec 修复与逐源验收

以下每个步骤均单独核对官方类型定义、修改或登记对应资源文件、执行 JSON 加载和静态门禁，并保留独立结果。S-34、S-42、S-72、S-74 没有可安全修改的实际二进制浮点承载，因此只登记了 source-file/type 级 allowlist，没有伪造 TapFloat/TapDouble 代码提交。

- S-18 Alibaba 1688：API/JavaScript `Number` 为 binary64，`Float` 映射 `TapDouble`，补齐 64/8/15 和 binaryPrecision=53。提交：`tapdata-connectors` `56615f22 feat(TAP-12568): fix ali1688 floating point spec`
- S-19 Shein：同样按 JavaScript `Number` binary64 处理展示型 `Float`，禁止按名称误判为单精度。提交：`4c92db61 feat(TAP-12568): fix shein floating point spec`
- S-20 Apache Hive1：`FLOAT`=binary32、`DOUBLE`=binary64，删除旧 scale。提交：`834f4130 feat(TAP-12568): fix hive1 floating point spec`
- S-21 Apache Hive3：独立修复 `float`/`double` 的 32/64 位属性。提交：`dc2367ab feat(TAP-12568): fix hive3 floating point spec`
- S-22 Apache Hudi：按 Hudi/Parquet 的 float/double 物理类型修复。提交：`f14ea474 feat(TAP-12568): fix hudi floating point spec`
- S-23 MRS Hive3：独立核对 MRS Hive3 定义，修复 float/double。提交：`886c6a81 feat(TAP-12568): fix mrs hive3 floating point spec`
- S-24 AnalyticDB MySQL：按 MySQL FLOAT/DOUBLE 及 M,D 语义修复，M,D 不进入平台 scale。提交：`4b0117e9 feat(TAP-12568): fix adb mysql floating point spec`
- S-25 AnalyticDB PostgreSQL：按 PostgreSQL real/double precision 修复。提交：`f9cc2c3b feat(TAP-12568): fix adb postgres floating point spec`
- S-26 ApsaraDB MongoDB：BSON Double 改为 `TapDouble`，移除 fixed/precision 错误描述。提交：`cba0a4b8 feat(TAP-12568): fix aliyun mongodb floating point spec`
- S-27 ApsaraDB RDS MariaDB：按 MariaDB FLOAT/DOUBLE 规则修复静态和参数化条目。提交：`0161bfc3 feat(TAP-12568): fix aliyun rds mariadb floating point spec`
- S-28 ApsaraDB RDS MySQL：按 MySQL FLOAT/DOUBLE 规则修复静态和参数化条目。提交：`0a923cf8 feat(TAP-12568): fix aliyun rds mysql floating point spec`
- S-29 ApsaraDB RDS PostgreSQL：按 PostgreSQL real/double precision 修复。提交：`c6689d65 feat(TAP-12568): fix aliyun rds postgres floating point spec`
- S-30 Amazon RDS MySQL：按 MySQL 官方规则修复 FLOAT/DOUBLE 和 M,D。提交：`4fa15506 feat(TAP-12568): fix aws rds mysql floating point spec`
- S-31 Azure Cosmos DB：JSON/SDK 数字承载按 binary64 处理，Float 仅保留为明确 binary32 的类型。提交：`9ebbd357 feat(TAP-12568): fix azure cosmos floating point spec`
- S-32 BigQuery：`spec.json` 与 `spec-v2.json` 同步将 FLOAT64 修复为 64/8/15 的 `TapDouble`。提交：`c25631bc feat(TAP-12568): fix bigquery floating point specs`
- S-33 ClickHouse：`Float32` -> `TapFloat`、`Float64` -> `TapDouble`，忽略无物理意义的类型参数。提交：`74e2b2bf feat(TAP-12568): fix clickhouse floating point spec`
- S-34 Coding：两份 spec 的逻辑 `Float` 没有可确认的 Java Float/binary32 承载证据；登记官方 allowlist，阻止名称推断；无 source JSON 修改、无代码提交。
- S-35 Custom Connection：明确自定义连接器 Float/Double 的平台映射并修复属性。提交：`e92ced49 feat(TAP-12568): fix custom connection floating point spec`
- S-36 Databend：修复 Float/Double 及参数化条目，移除 scale。提交：`660fd42a feat(TAP-12568): fix databend floating point spec`
- S-37 Apache Doris：FLOAT=32/4/7、DOUBLE=64/8/15。提交：`bc247762 feat(TAP-12568): fix doris floating point spec`
- S-38 GaussDB(DWS)：按 PostgreSQL 派生类型修复 real/double precision。提交：`e47752f5 feat(TAP-12568): fix dws floating point spec`
- S-39 Elasticsearch：float/double 分别修复；`half_float` 16 位、`scaled_float` scaled long 进入显式 allowlist。提交：`84c7262a feat(TAP-12568): fix elasticsearch floating point spec`
- S-40 Excel：按 Apache POI/Excel Double 承载改为 `TapDouble`，删除 fixed/scale。提交：`31f2d623 feat(TAP-12568): fix excel floating point spec`
- S-41 Greenplum：按 PostgreSQL real/double precision 修复。提交：`e5391025 feat(TAP-12568): fix greenplum floating point spec`
- S-42 HBase：connector 将 cell bytes 读为字符串，没有实际 float/double 二进制解码路径；登记 float/double allowlist，无 source JSON 修改、无代码提交。
- S-43 HighGo：按 PostgreSQL 兼容类型修复 real/double precision。提交：`f2996a90 feat(TAP-12568): fix highgo floating point spec`
- S-44 Huawei Cloud GaussDB：修复 real/double precision 及 float4/float8 别名。提交：`1993e119 feat(TAP-12568): fix huawei gaussdb floating point spec`
- S-45 Confluent Avro：Avro float/double 按协议修复为 32/64 位。提交：`f235206b feat(TAP-12568): fix kafka avro floating point spec`
- S-46 Kafka Enhanced：按 Confluent compatible schema 独立修复消息 codec 对应类型。提交：`ae1eaf88 feat(TAP-12568): fix kafka enhanced floating point spec`
- S-47 MariaDB：按 MariaDB FLOAT/DOUBLE 规则修复静态和参数化条目。提交：`889cbc26 feat(TAP-12568): fix mariadb floating point spec`
- S-48 MongoDB Atlas：BSON Double -> `TapDouble`，移除定点属性。提交：`f56ad594 feat(TAP-12568): fix mongodb atlas floating point spec`
- S-49 MongoDB：BSON Double -> `TapDouble`，Decimal128 保持本需求范围外。提交：`77fa3566 feat(TAP-12568): fix mongodb floating point spec`
- S-50 MongoDB Below 3.4：按旧驱动 BSON Double 能力修复为 `TapDouble`。提交：`9a3207d1 feat(TAP-12568): fix mongodb lower floating point spec`
- S-51 MySQL：全量修复 FLOAT/DOUBLE、FLOAT(p) 和 FLOAT(M,D)，DOUBLE 不再携带 scale。提交：`f6d5b520 feat(TAP-12568): fix mysql floating point spec`
- S-52 MySQL PXC：独立复核 PXC 驱动无额外宽度差异，按 MySQL 定义修复。提交：`1ee25143 feat(TAP-12568): fix mysql pxc floating point spec`
- S-53 OceanBase MySQL：按 MySQL 兼容定义修复 FLOAT/DOUBLE 及参数化条目。提交：`025bc8b8 feat(TAP-12568): fix oceanbase mysql floating point spec`
- S-54 openGauss：按官方 real/double precision 修复并补齐有效位数。提交：`a62e34fb feat(TAP-12568): fix opengauss floating point spec`
- S-55 Apache Paimon Plus：修复 FLOAT/DOUBLE 的 32/64 位描述，删除 precision/scale 定点误描述。提交：`52b78ba5 feat(TAP-12568): fix paimon floating point spec`
- S-56 PolarDB MySQL：按 MySQL FLOAT/DOUBLE 规则修复静态和参数化条目。提交：`e0335770 feat(TAP-12568): fix polardb mysql floating point spec`
- S-57 PolarDB PostgreSQL：按 PostgreSQL real/double precision 修复。提交：`57c552b0 feat(TAP-12568): fix polardb postgres floating point spec`
- S-58 PostgreSQL：real=32、double precision=64，修复旧 real bit=4 和 scale。提交：`358be6dd feat(TAP-12568): fix postgresql floating point spec`
- S-59 QuickAPI：核对 API JSON/Java double 为 binary64，修复为 `TapDouble`。提交：`e8ed1e15 feat(TAP-12568): fix quickapi floating point spec`
- S-60 Redis：区分数值文本与 binary 类型，按 connector 实际承载修复。提交：`b9648fa6 feat(TAP-12568): fix redis floating point spec`
- S-61 RisingWave：按 PostgreSQL 派生 real/double precision 修复。提交：`b5f2eb8c feat(TAP-12568): fix risingwave floating point spec`
- S-62 SelectDB：FLOAT=32、DOUBLE=64，删除 float 的旧 scale。提交：`f65b16fc feat(TAP-12568): fix selectdb floating point spec`
- S-63 Snowflake：FLOAT 修复为 binary64 `TapDouble`；DECFLOAT 保持 decimal 范围外并登记 allowlist。提交：`b22cf4fa feat(TAP-12568): fix snowflake floating point spec`
- S-64 StarRocks：FLOAT/DOUBLE 修复为 32/64 位，作为 SQL Server float(53) 目标端回归类型。提交：`3ca5a603 feat(TAP-12568): fix starrocks floating point spec`
- S-65 Tablestore：按 SDK Double 实际承载修复为 `TapDouble`，移除 scale。提交：`e2536245 feat(TAP-12568): fix tablestore floating point spec`
- S-66 TDengine：FLOAT=32、DOUBLE=64，补齐有效位数和范围。提交：`44417185 feat(TAP-12568): fix tdengine floating point spec`
- S-67 TencentDB MariaDB：按 MariaDB/MySQL 兼容规则修复 FLOAT/DOUBLE 和 M,D。提交：`aa7c8ad2 feat(TAP-12568): fix tencent mariadb floating point spec`
- S-68 TencentDB MongoDB：BSON Double -> `TapDouble`，删除错误 fixed/precision/scale。提交：`04b5099f feat(TAP-12568): fix tencent mongodb floating point spec`
- S-69 TencentDB PostgreSQL：按 PostgreSQL real/double precision 修复。提交：`3823fb00 feat(TAP-12568): fix tencent postgres floating point spec`
- S-70 TiDB：按 MySQL/TiDB FLOAT/DOUBLE 定义修复静态和参数化条目。提交：`97913f72 feat(TAP-12568): fix tidb floating point spec`
- S-71 Vastbase G100：按 PostgreSQL 兼容 real/double precision 修复。提交：`adfded91 feat(TAP-12568): fix vastbase floating point spec`
- S-72 vika：`SingleText` 为误命中，不是浮点候选；登记 false-positive 结论，不修改 source JSON、无代码提交。
- S-73 YashanDB：按官方 FLOAT/DOUBLE 及 `p` 范围实现独立 resolver：`p=1..23` 单精度、`p=24..126` 双精度，默认 126。提交：`3149fc6d feat(TAP-12568): fix yashandb floating point spec`
- S-74 Zoho Desk：官方 API 未证明逻辑 `Float` 是 binary32，登记 allowlist 阻止名称推断；无 source JSON 修改、无代码提交。

- 验证：S-18 至 S-74 对应的 76 个 spec 文件均通过 JSON 解析；静态门禁共检查 166 个浮点候选，所有未映射到新类型的候选均有 source-file/type 级官方 allowlist。

## P-02 公共类型层次与协议兼容

- 状态：已完成
- 完成内容：在 `tapdata-common-lib` 增加 `TapFloat`、`TapDouble` 及对应值类；二者直接继承 `TapType`，不继承 `TapNumber`；保留 `TYPE_NUMBER=8`，新增 `typeName` 识别；增加 bit、storageBytes、effectivePrecision、binaryPrecision、有限范围和 NaN/Infinity 能力属性，并限制错误的 bit/fixed 设置。
- 兼容结论：旧的数字协议仍按 `TYPE_NUMBER` 恢复 `TapNumber`；新类型通过 `typeName` 区分，旧模型不会因新增类而改变解析路径。
- 验证：先运行失败测试确认新类不存在；实现后 `mvn -q -Dtest=TapFloatingPointTypeTest test` 通过，`git diff --check` 通过。
- 提交：`tapdata-common-lib` `b2f01dc feat(TAP-12568): add TapFloat and TapDouble schema types`

## P-03 浮点值对象承载与量化

- 状态：已完成
- 完成内容：`TapFloatValue`、`TapDoubleValue` 均以 `Double` 作为平台承载；`TapFloatValue` 在构造器、`value(...)` 和 `setValue(...)` 中统一执行 binary32 量化，避免反射/反序列化路径绕过精度约束；`TapDoubleValue` 保持 binary64 值。
- 验证：先补充构造器、链式 setter、普通 setter 三条入口的失败测试；实现后 `mvn -q -Dtest=TapFloatingPointTypeTest test` 通过，`git diff --check` 通过。
- 提交：`tapdata-common-lib` `184f800 feat(TAP-12568): enforce float value quantization`

## P-04 入站/出站 codec 与默认注册

- 状态：已完成
- 完成内容：新增 `ToTapFloatCodec`、`ToTapDoubleCodec`、`FromTapFloatCodec`、`FromTapDoubleCodec`；入站按目标类型转换，单精度统一 binary32 量化，出站分别返回 `Float`/`Double`；特殊值能力由 schema 的 `supportsNaN`/`supportsInfinity` 控制；旧 `TapNumber` codec 未修改其历史行为；默认 codec 映射增加新值类。
- 验证：先运行缺失 codec 的编译失败测试；实现后浮点入站精度、出站 Java 类型、特殊值拒绝测试通过；`git diff --check` 通过。
- 提交：`tapdata-common-lib` `ecb3e4f feat(TAP-12568): add floating point codecs`

## P-05 Java 类型推导

- 状态：已完成
- 完成内容：`JavaTypesToTapTypes` 改为 `Float -> TapFloat`、`Double -> TapDouble`；移除二者生成 `TapNumber.scale/precision` 的旧推断；`BigDecimal`、整数和其他 `Number` 保持 `TapNumber`；补齐按 `TapFloat.class`/`TapDouble.class` 反向创建类型的分支。
- 验证：先确认旧实现对 Float/Double 的两类测试失败；修改后 `mvn -q -Dtest=JavaTypesToTapTypesTest,TapFloatingPointTypeTest test` 通过，`git diff --check` 通过。
- 提交：`tapdata-common-lib` `7e9829c feat(TAP-12568): infer dedicated Java floating point types`

## P-06 浮点 mapping 与参数化 precision resolver

- 状态：已完成
- 完成内容：新增 `TapFloatMapping`、`TapDoubleMapping` 和 `TapFloatingPointMapping`；静态 mapping 可描述位宽、存储字节、有效精度、二进制 precision、范围和特殊值能力；参数化 mapping 支持 `precision <= 24 -> TapFloat`、`precision >= 25 -> TapDouble`，默认 53；`TapMapping.build` 增加可选 `mapping` 类选择；`TapNumberMapping` 通过兄弟类型隔离，不会匹配新浮点类型。
- 验证：先确认 mapping 类缺失导致测试编译失败；实现后静态 mapping、参数分流、默认双精度和旧 mapping 隔离测试通过；`git diff --check` 通过。
- 提交：`tapdata-common-lib` `2d3cf35 feat(TAP-12568): add floating point mappings`

## P-07 Jackson JSON 类型名与旧协议兼容

- 状态：已完成
- 完成内容：引擎 `JSONUtil`、管理端 `JsonUtil` 的 `TapType` deserializer 均优先读取 `typeName`，再回退到历史 `type=8`；新类型序列化自动输出 `typeName`；旧 numeric-only payload 继续恢复 `TapNumber`，没有新增协议 byte。
- 验证：`iengine-common` 的 `JSONUtilTest` 和 `tm-common` 的 `JsonUtilTest` 均通过，覆盖 `TapFloat`/`TapDouble` 往返及旧 `TapNumber` payload；`git diff --check` 通过。
- 提交：`tapdata` `ad92a69df5 feat(TAP-12568): preserve floating point type names in JSON`

## P-08 PDK/Fastjson 类型分派

- 状态：已完成
- 完成内容：PDK `TapConstants.abstractClassDetectors` 增加 `typeName=TapFloat/TapDouble` detector，并置于历史 `type=8 -> TapNumber` detector 之前；保留旧 Fastjson numeric-only 解析路径。
- 验证：先运行测试确认新 JSON 被错误解析成 `TapNumber`；修复后 Fastjson 新类型解析、旧类型回退和 detector 顺序测试通过；`git diff --check` 通过。
- 提交：`tapdata-common-lib` `a1b3142 feat(TAP-12568): support floating point type names in PDK JSON`

## P-09 旧模型 TapNumber 兼容解析

- 状态：已完成
- 完成内容：新增 `LegacyTapTypeResolver` 和 `LegacyTapTypeResolution`；对于旧模型中的 `TapNumber`，仅使用已加载的 source spec 进行浮点规范化；参数化类型按实际 precision 分流；decimal/整数继续保留 `TapNumber`；疑似浮点但缺少可靠 spec 时返回 `LEGACY_FLOAT_TYPE_UNRESOLVED`，不默认成双精度、不触发模型重载。
- 验证：覆盖明确单精度、参数化 53 位双精度、精确数值保留及无 spec 歧义四种情况；`mvn -q -Dtest=LegacyTapTypeResolverTest test` 通过，`git diff --check` 通过。
- 提交：`tapdata-common-lib` `ea7c390 feat(TAP-12568): add legacy floating point resolver`

## P-10a 公共过滤器 numeric codec 分派

- 状态：已完成（P-10 平台转换子步）
- 完成内容：`TapCodecsFilterManager` 对历史 `TYPE_NUMBER=8` 改为调用具体 `TapType.toTapValueCodec()`；因此 `TapFloat`/`TapDouble` 不再掉回 `TapNumber` codec，`TapCodecsFilterManagerSchemaEnforced` 复用该分派路径。
- 验证：先以匿名 `TapFloat` codec 写入失败测试；修改后 `mvn -q -Dtest=TapCodecsFilterManagerTest test` 通过，`git diff --check` 通过。
- 提交：`tapdata-common-lib` `3c18aea feat(TAP-12568): dispatch numeric codecs by concrete type`

## P-10 引擎与管理端具体类型处理

- 状态：已完成（代码已提交；全仓库验证受既有基线问题阻断）
- 完成内容：管理端 `PdkSchemaConvert` 按 `typeName` 恢复具体浮点类；字段简单类型支持 Float/Double；Union schema 合并按 32/64 bit 比较；预览 mock、测试输入转换、CDC 条件值、自动比对、共享 CDC 过滤均识别 `TapFloat`/`TapDouble`；未改变 `TapNumber` 的整数/定点逻辑。
- 验证：`tm-common` 的 `PdkSchemaConvertTest` 通过。`iengine-app` 编译未能完成，阻断项为仓库既有 `ObsLogger.alert`、`ENSURE_TABLES_FAILED` 和 `ShareCdcTaskPdkContext` API 不一致；`tm` 编译未能完成，阻断项为仓库既有 SSO 缺失类。上述错误不涉及本次浮点代码；本次改动本身通过 `git diff --check`。
- 提交：`tapdata` `887bfe7813 feat(TAP-12568): adapt engine and manager numeric handling`

## P-11 数据源 spec 静态质量门禁

- 状态：已完成
- 完成内容：新增独立 Python 检查器和 JSON allowlist；检查两个连接器仓库的 spec 时，强制验证 `TapFloat`/`TapDouble` 的 bit、storageBytes、effectivePrecision，禁止二进制浮点继续携带 decimal precision/scale/fixed 属性，并阻止未登记的浮点候选继续映射到 `TapNumber`。Oracle `FLOAT(p)`、Elasticsearch `half_float`/`scaled_float`、HBase 未解码的字节类型及无法确认的 API 逻辑名称必须带官方链接、理由和 source-file/type 级 allowlist。
- 自审结论：门禁显式把 64 位误标单精度、32 位误标双精度、定点属性残留和未登记候选作为失败项；allowlist 只用于官方定义不是 binary32/binary64 或连接器没有实际二进制承载证据的情况，不能作为默认降级。
- 验证：对现有 MySQL spec 运行检查器，按预期报告 4 个待修复候选；`git diff --check` 通过。
- 提交：`tapdata` `cbffea79d6 feat(TAP-12568): add floating point spec validator`
