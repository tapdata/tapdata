# TAP-12615 D06 实施计划

## 目标

将 Engine 的批次/事件结果回调落到 TM 的批次和事件状态机，确保回调只能推进合法状态，并在事件归属和批次计数不满足条件时拒绝更新。

## 实施步骤

- [x] 核对 A02 状态机、D05 消息字段和现有 Repository 条件更新。
- [x] 先增加 `BATCH_STARTED`、`EVENT_STARTED`、事件归属失败和批次计数不一致的 RED 用例。
- [x] 新增事件启动条件更新，追加 `RUNNING` attempt 并刷新 TTL。
- [x] 实现五类回调的批次状态校验、事件结果迁移和批次计数对账。
- [x] 为非法结果、错误批次状态和事件锁归属失败定义稳定错误码。
- [x] 更新 D06 进度、开发计划和详细设计。
- [x] 执行定向回归并创建本地 commit。

## 验证

- `DqlEventRepositoryTest` 覆盖 `REPROCESSING + current_batch_id` 条件下的 `RUNNING` attempt 追加和 TTL 刷新。
- `DqlRecoveryBatchServiceTest` 覆盖批次启动、事件启动、事件结果归属失败、计数不一致和既有跳过/批次失败路径。
- D06 定向回归 51 项全部通过。

## 后续

D07 在本状态机之上增加以 `attemptId + batchId + eventId` 为边界的回调幂等，防止重复回调重复追加 attempt、增加 `recovery_count` 或增加批次计数。
