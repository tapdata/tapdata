# TAP-12615 E01 实施计划

## 目标

为 Engine 建立独立的 `dqlRecovery` WebSocket 消息边界：完成消息解析、任务上下文校验、跨 Handler 幂等 claim 和 `BATCH_STARTED` 回调适配，为后续实际回放协调器提供稳定入口。

## 实施内容

- [x] 复核 TM `DqlRecoveryMessageDto` 和 recovery callback 契约。
- [x] 增加 typed message parser 和输入边界校验。
- [x] 增加任务版本、Agent 和任务归属校验。
- [x] 增加进程级批次 claim，覆盖 WebSocket Handler 重建场景。
- [x] 增加 `BATCH_STARTED` 回调 Client/Reporter 和 WebSocket 适配。
- [x] 增加正常、非法、重复、失败重试和跨 Handler 幂等测试。
- [x] 更新步骤文档、开发计划并创建本地 commit。

## 验证

Engine 依赖闭包定向回归共 22 项通过：TM Client 5 项、Reporter 8 项、消息 Handler 7 项、Parser 2 项，`BUILD SUCCESS`。

## 后续

自动进入 E02，实现 `TapdataDqlRecoveryEvent` 的快照重建和恢复元数据保留。
