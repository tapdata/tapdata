# TAP-12615 D05 实施计划

## 目标

定义独立 `dqlRecovery` 消息 DTO，按 D02 顺序下发到批次 Agent，并与 D04 的 `DISPATCHED/FAILED` 生命周期及补偿保持一致。

## 实施步骤

- [x] 核对详细设计、TM pipe/WebSocket 传输和 Engine handler 路由约定。
- [x] 编写消息 DTO payload、禁止 DataSync `opType` 和 TM 下发顺序测试。
- [x] 在 `tm-common` 新增 `DqlRecoveryMessageDto` 及批次到 payload 的转换。
- [x] 在 `DqlRecoveryBatchService` 使用 DTO 下发完整字段，并先更新批次为 `DISPATCHED`。
- [x] 更新 D05 进度、开发计划和详细设计。
- [x] 执行 D05 定向回归并创建本地 commit。

## 验证

- 消息 DTO 契约测试通过。
- TM 批次服务验证 `DISPATCHED` 更新先于消息发送。
- 下发异常仍执行 D04 事件、批次和任务锁补偿。
