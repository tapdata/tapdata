# TAP-12615 D10 实施计划

## 目标

把 TM 重处理批次控制的并发、锁冲突、下发失败、重复回调、部分失败、批次失败和超时补偿纳入统一自动化回归。

## 实施步骤

- [x] 盘点 D01-D09 已有 Service、Repository、定时任务和幂等测试。
- [x] 增加同一任务并发发起测试，证明只有一个请求可以取得任务锁。
- [x] 增加部分失败终态测试，验证告警和任务锁释放。
- [x] 执行 TM 重处理定向回归。
- [x] 更新 D10 步骤文档、开发计划和阶段 M3 里程碑记录。
- [x] 执行完整 DQL 回归并创建本地 commit。

## 验证

D10 定向回归 81 项通过；包含 D09 新增详情/审计测试的完整 DQL 回归 130 项通过，`BUILD SUCCESS`。

## 后续

阶段 D 已完成。E01 开始实现 Engine 对 `type=dqlRecovery` 的独立消息接收、校验、幂等拒绝和 `BATCH_STARTED` 回调。
